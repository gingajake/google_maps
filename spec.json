{
  "nio/GoogleMapsGeocode": {
    "version": "0.1.0",
    "description": "",
    "properties": {
      "address": {
        "title": "Street Address",
        "type": "Type",
        "description": "Street address to be converted into geographic coordinates",
        "default": ""
      },
      "api_key": {
        "title": "API Key",
        "type": "Type",
        "description": "API Key for the Google Maps API",
        "default": "[[GOOGLE_MAPS_API_KEY]]"
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals"
      }
    },
    "outputs": {
      "default": {
        "description": "The input list of signals plus latitude and longitude for the configured address"
      }
    },
    "commands": {}
  }
}
